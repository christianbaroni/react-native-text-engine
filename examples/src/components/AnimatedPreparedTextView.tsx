import Animated from 'react-native-reanimated';
import { PreparedTextView } from 'react-native-text-engine';

export const AnimatedPreparedTextView = Animated.createAnimatedComponent(PreparedTextView);
